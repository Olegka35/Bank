apiVersion: v1
kind: Pod
metadata:
  name: {{ .Chart.Name }}-test
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['http://api-gateway:8080/actuator/health']
  restartPolicy: Never